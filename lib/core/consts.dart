import 'package:flutter/material.dart';

const double PADDING = 18;
const BorderRadius RADIUS = BorderRadius.all(Radius.circular(4));
const Duration ANIMATION = Duration(milliseconds: 300);
const Curve CURVE = Curves.easeIn;

const String KILLERGAMES_LOGO =
    'assets/images/Killer_Games_logo_transparent.png';
